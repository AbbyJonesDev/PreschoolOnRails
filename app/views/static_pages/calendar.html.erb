<!-- Calendar -->
<main class="row">
  <% if @calendar %>
    <%= render partial: "layouts/embedded_pdf", locals: { file: @calendar.calendar_file } %>
  <% else %>
    <div class="panel">
      No calendar available at this time.
    </div>
  <% end %>
</main>