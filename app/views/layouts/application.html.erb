<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="google-site-verification" content="pVTrtoTcbxUFV9WO6I5-c81-yV_Y6njj3DzcQ0b6mqg"/>
    <title><%= content_for?(:title) ? yield(:title) : "Here We Grow" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <%= render 'shared/ga' %> 
  </head>

  <body class=<%= @body_background %> >

    <header>
      <%= render_banner %>
    </header>

    <%= render 'layouts/flash_messages' %>


    <%= yield %>

    <footer>
      <%= render_footer %>
    </footer>

    <%= javascript_include_tag "application" %>
  </body>
</html>