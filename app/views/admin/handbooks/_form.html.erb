<%= form_for [:admin, @handbook], multipart: true do |f| %>

  <% if @handbook.errors.any? %>
    <div id="error_explanation">
      <p>
        <%= pluralize(@handbook.errors.count, "error") %> prevented this handbook from being saved:
      </p>
      <ul>
        <% @handbook.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="medium-12 columns">
      <%= f.label :year, "Year:" %>
      <%= f.text_field :year, placeholder: "20XX-20XX" %>
    </div>
  </div>

  <div class="row">
    <div class="medium-12 columns">
      <%= f.file_field :calendar_file %>
    </div>
  </div>

  <br>

  <div class="row">
    <div class="medium-2 columns">
      <%= f.submit "Submit" %>
    </div>
    <div class="medium-2 columns">
      <%= link_to "Cancel", admin_calendars_path %>
    </div>
  </div>
<% end %>